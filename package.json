{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "build": "lage build --grouped",
    "cover": "lage cover --grouped",
    "test": "lage test --grouped",
    "lint": "lage lint --grouped",
    "change": "beachball change --no-commit",
    "hello": "pnpm --filter sample-a start",
    "publish": "beachball publish --new"
  },
  "devDependencies": {
    "lage" : "^2.7.5",
    "beachball": "^2.35.0"
  }
}